#read in as df
#normalize each sample column
#count number of sample columns, first half is female, other half is male
#take mean over each sex for each line
#print file with same format but only "two" sample columns, one for female, one for male
